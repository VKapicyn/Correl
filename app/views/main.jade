html
    head
        meta(charset='utf-8')
        meta(http-equiv='X-UA-Compatible', content='IE=edge')
        meta(name='viewport', content='width=device-width, initial-scale=1')

        // Styles
        link(rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css")
        link(rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap-theme.min.css")
        link(href='/css/styles.css' rel="stylesheet" type="text/css")

        //JS
        script(src='/js/jquery-3.1.1.min.js')
        script(src="//netdna.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js")
        script(src='/js/front.js')
        title
            | general
    body
        .container
            .row
                .col-md-3
                    form(method='post', action='/result')
                        input(type='text', name='kf', placeholder='Коэффициент')
                        input(type='text', name='period', placeholder='Период в месяцах')  
                        br
                        input(type='text', name='procent', placeholder='Процент') 
                        br
                        select(name='price')
                            option Закрытие
                            option Медиана
                        br
                        textarea(name='tickers', rows='5', cols='15', placeholder='список тикеров')
                        br
                        button(type='input') Отправить
                        br
                        hr
                    | Коэффициент: #{data.kf}
                    br
                    | Период: #{data.period}
                    br
                    | Процент: #{data.procent}
                    br
                    | Тип цены: #{data.price}
                    br
                    hr
                    | Почта: #{data.email}
                    br
                    form(method='post', action='/email')
                        input(name='email', type='text', placeholder='Почта')
                        button(type='input') Обновить
                .col-md-9
                    table.table
                        thead.thead-inverse
                        tr
                            th #
                            th 
                                a(href='#' onclick='toSort()') Коэфф.
                            th Тикер #1
                            th Тикер #2
                        tbody(id='sTable')
                                        tr
